echo "Building with SDK from $ORCHESTRA_SDK_ROOT in Release mode"
mkdir -p build
cd build
cmake ../ -DORCHESTRA_SDK_ROOT=$ORCHESTRA_SDK_ROOT -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=${HOME}/local/
make -j8 
make install